"""Exercise: choose a vampire route at random.

This picks one of several possible actions.
"""

import random

routes = ["hide in the crypt", "run to the forest", "open the old gate"]
print(random.choice(routes))
